"use client";

import React, { memo } from "react";
import { Stack } from "@mui/material";

type TimelineProps = {};

const Timeline = (props: TimelineProps) => {
  return <Stack>Timeline</Stack>;
};

export default memo(Timeline);
