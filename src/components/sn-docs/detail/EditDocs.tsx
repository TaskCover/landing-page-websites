import ChatEditor from "components/sn-chat/components/chat/ChatEditor";
import React, { memo } from "react";

const EditDocs = () => {
  return (
    <div>
      <ChatEditor></ChatEditor>
    </div>
  );
};

export default memo(EditDocs);
