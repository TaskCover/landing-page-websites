export type TimeTrackingDictionary = {
  header: {
    tab: {
      myTime: string;
      companyTime: string;
      workLog: string;
      workTime: string;
      breakTime: string;
    };
  };
  myTime: {
    addButton: string;
  };
};
